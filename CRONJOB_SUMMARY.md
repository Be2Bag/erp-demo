# สรุปการสร้าง Cronjob สำหรับตรวจสอบสถานะ Payable/Receivable

## ✅ สิ่งที่สร้างเสร็จแล้ว

### 1. **Cronjob Status Checker** (`cron/status_checker.go`)
- ตรวจสอบและอัปเดตสถานะของ **Payable** (เจ้าหนี้) และ **Receivable** (ลูกหนี้) อัตโนมัติ
- รันทุกวันเวลา **00:00 น. (เที่ยงคืน)**
- มีฟังก์ชัน `RunNow()` สำหรับรันด้วยตนเองโดยไม่ต้องรอถึงเวลากำหนด

### 2. **API Endpoint สำหรับรันด้วยตนเอง** (`handlers/cron_handler.go`)
- **POST** `/service/api/cron/status-check`
- ต้อง authenticate ก่อนเรียกใช้
- สามารถเรียกใช้ผ่าน API เพื่อรันการตรวจสอบทันที

### 3. **เอกสาร README** (`cron/README.md`)
- คำอธิบายละเอียดเกี่ยวกับการทำงานของ cronjob
- ตัวอย่างการใช้งาน
- อธิบายสถานะต่างๆ และโลจิกการตรวจสอบ

### 4. **อัปเดต main.go**
- เพิ่มการเริ่มต้น cronjob เมื่อ server เริ่มทำงาน
- เพิ่ม route สำหรับ manual trigger

## 📋 สถานะที่ระบบจัดการ

| สถานะ | คำอธิบาย | เงื่อนไข |
|-------|----------|----------|
| **pending** | รอดำเนินการ | ยังไม่มีการชำระเงินเลย และยังไม่เกินกำหนด |
| **partial** | ชำระบางส่วน | มีการชำระบางส่วน (0 < Balance < Amount) และยังไม่เกินกำหนด |
| **paid** | ชำระครบแล้ว | Balance = 0 (ตั้งค่าเมื่อบันทึกการชำระครบ) |
| **overdue** | เกินกำหนด | เลยวันครบกำหนด (DueDate < วันปัจจุบัน) และยังมียอดคงเหลือ |

## 🚀 วิธีใช้งาน

### 1. เริ่มต้นอัตโนมัติ
เมื่อ server เริ่มทำงาน cronjob จะเริ่มต้นอัตโนมัติและทำงานทุกวันเวลา 00:00 น.

```bash
go run cmd/main.go
```

คุณจะเห็น log:
```
[CRON] Status Checker เริ่มทำงานแล้ว (รันทุกวันเวลา 00:00 น.)
Cronjob เริ่มทำงานแล้ว - ระบบจะตรวจสอบสถานะ Payable/Receivable ทุกวันเวลา 00:00 น.
```

### 2. รันด้วยตนเองผ่าน API

**Endpoint**: `POST /service/api/cron/status-check`

**ตัวอย่าง cURL:**
```bash
curl -X POST http://localhost:3000/service/api/cron/status-check \
  -H "Cookie: auth_token=YOUR_AUTH_TOKEN"
```

**Response สำเร็จ:**
```json
{
  "success": true,
  "message": "รัน cronjob สำเร็จ - ตรวจสอบ log เพื่อดูรายละเอียด"
}
```

**Response ล้มเหลว:**
```json
{
  "success": false,
  "message": "รัน cronjob ไม่สำเร็จ",
  "error": "error message"
}
```

## 📊 โลจิกการตรวจสอบ

### สำหรับทั้ง Payable และ Receivable:

1. **ดึงข้อมูล**: ดึงเฉพาะรายการที่:
   - ยังไม่ถูกลบ (deleted_at = null)
   - สถานะเป็น `pending` หรือ `partial`
   - ยังมียอดคงเหลือ (Balance > 0)

2. **ตรวจสอบวันครบกำหนด**:
   - ✅ **ถ้าเลยกำหนด** (DueDate < วันปัจจุบัน) และ Balance > 0 → เปลี่ยนเป็น **overdue**
   - ✅ **ถ้ายังไม่เลยกำหนด**:
     - Balance = Amount → **pending** (ยังไม่จ่าย/รับเลย)
     - 0 < Balance < Amount → **partial** (จ่าย/รับบางส่วน)

3. **บันทึกการเปลี่ยนแปลง**: อัปเดตสถานะและ UpdatedAt

## 📝 Log Messages ที่คุณจะเห็น

### เมื่อรันการตรวจสอบ:
```
[CRON] เริ่มตรวจสอบสถานะ Payable และ Receivable...
[CRON] อัปเดต Payable INV-2024-001: partial → overdue
[CRON] ตรวจสอบ Payable เสร็จสิ้น: อัปเดต 3 รายการ จากทั้งหมด 10 รายการ
[CRON] อัปเดต Receivable REC-2024-005: pending → overdue
[CRON] ตรวจสอบ Receivable เสร็จสิ้น: อัปเดต 2 รายการ จากทั้งหมด 8 รายการ
[CRON] ตรวจสอบสถานะเสร็จสิ้น
```

### เมื่อเกิด Error:
```
[CRON ERROR] ตรวจสอบ Payable ไม่สำเร็จ: <error message>
[CRON ERROR] อัปเดต Receivable <id> ไม่สำเร็จ: <error message>
```

## 🎯 ตัวอย่างการทำงาน

### Scenario 1: ใบแจ้งหนี้เลยกำหนด
- **วันที่สร้าง**: 2024-01-01
- **วันครบกำหนด**: 2024-01-15
- **จำนวนเงิน**: 10,000 บาท
- **ยอดคงเหลือ**: 10,000 บาท
- **สถานะเริ่มต้น**: pending

➡️ เมื่อถึง **2024-01-16 (00:00 น.)** → สถานะเปลี่ยนเป็น **overdue**

### Scenario 2: ชำระบางส่วนและเลยกำหนด
- **จำนวนเงิน**: 10,000 บาท
- **ยอดคงเหลือ**: 5,000 บาท (ชำระไป 5,000)
- **สถานะปัจจุบัน**: partial
- **วันครบกำหนด**: 2024-01-15

➡️ เมื่อถึง **2024-01-16 (00:00 น.)** → สถานะเปลี่ยนเป็น **overdue**

### Scenario 3: ชำระครบถ้วน
- **จำนวนเงิน**: 10,000 บาท
- **ยอดคงเหลือ**: 0 บาท
- **สถานะ**: paid

➡️ Cronjob **จะไม่ดึงข้อมูลนี้มาตรวจสอบ** (เพราะ Balance = 0)

## ⚙️ การปรับแต่งเวลารัน

หากต้องการเปลี่ยนเวลาในการรัน สามารถแก้ไขได้ที่ `cron/status_checker.go`:

```go
// รันทุกวันเวลา 01:00 น.
_, err := sc.cron.AddFunc("0 0 1 * * *", func() { ... })

// รันทุก 6 ชั่วโมง
_, err := sc.cron.AddFunc("0 0 */6 * * *", func() { ... })

// รันทุกชั่วโมง
_, err := sc.cron.AddFunc("0 0 * * * *", func() { ... })
```

**รูปแบบ Cron Expression**: `second minute hour day month weekday`

## 📦 Dependencies ที่ติดตั้งแล้ว

```bash
go get github.com/robfig/cron/v3
```

## 📁 ไฟล์ที่สร้าง

```
cron/
  ├── status_checker.go    # Logic หลักของ cronjob
  └── README.md            # เอกสารคำอธิบาย

handlers/
  └── cron_handler.go      # API handler สำหรับรันด้วยตนเอง

cmd/
  └── main.go              # อัปเดตเพื่อเริ่มต้น cronjob
```

## ✨ คุณสมบัติเพิ่มเติม

1. **Error Handling**: หากอัปเดตรายการใดไม่สำเร็จ จะ skip และดำเนินการต่อ
2. **Performance**: ดึงเฉพาะรายการที่จำเป็นต้องตรวจสอบ
3. **Logging**: แสดง log ละเอียดเพื่อติดตามการทำงาน
4. **Manual Trigger**: สามารถรันด้วยตนเองผ่าน API
5. **Timezone**: ใช้ timezone ของ server

## 🧪 การทดสอบ

1. สร้างข้อมูล Payable/Receivable ที่มี DueDate ในอดีต
2. เรียก API `POST /service/api/cron/status-check`
3. ตรวจสอบ log และสถานะในฐานข้อมูล

## 📌 หมายเหตุสำคัญ

- ระบบจะตรวจสอบเฉพาะรายการที่ **ยังมียอดคงเหลือ** (Balance > 0)
- รายการที่ **ชำระครบแล้ว** (paid) จะไม่ถูกดึงมาตรวจสอบ
- สถานะ **paid** จะถูกตั้งค่าเมื่อมีการบันทึกการชำระเงินในระบบเท่านั้น
- Cronjob จะไม่เปลี่ยนสถานะจาก **overdue** กลับเป็น **pending** หรือ **partial** โดยอัตโนมัติ

---

✅ **ระบบพร้อมใช้งานแล้ว!** Cronjob จะทำงานอัตโนมัติทุกวันเวลา 00:00 น.
